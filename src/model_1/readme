1.model_1
    思路：将数据分为上下午两个，分别利用6、7预测8、9和15、16预测17、18，上午为例：由于预测的值为8:00-8:20,8:20-8:40,8:40-9:00,9:00:9:20,9:20-9:40,9:40-10:00,一共6个值，
    会导致误差较大，所以我们选择预测8：00-9：00的均值，9：00-10：00的均值，在利用历史的值来计算8:00-8:20,8:20-8:40,8:40-9:00分别对于8点均值的比例ratio，这样我们只要预测
    两个值就可以。同时我们选择了sklearn里面的ExtraTreesRegressor模型来连续预测多个标签
    运行该模型只要运行main文件夹下的run.py文件就可以完成原始数据到输出预测结果
    1）数据预处理data_processing文件：
        aggregate_volume.py  用于生成每一个20分钟的车流量，官方给了代码
        data_processing.py   用于过滤国庆节10-1号到10-7号的数据，同时将数据分为上下午，分为train和test，每一行表示一个样本，存储了一些基本的原始特征，如tollagate_id,
                             direction,month,day,week,以及67两个小时每20风中的6个值
    2）模型训练src/model_1文件
        feature.py： 抽取特征，抽取的特征有tollgateId特征onehot，direction特征onehot，week 周几onehot特征，原始每20分钟的值，原始一个小时内3个值的max，mean，median，
                     max，min，std等统计量，两个小时的均值的二项式，原始2个小时内6个值的max，mean，median，max，min，std等统计量，是否特殊天，比如工作日，工作第一天，
                     放假第一天，上班前一天，是否节假日等
        params.py:   利用网格搜索选择最优的参数
        test.py:     利用最后一周测试模型的效果
        predict.py:  预测模型的结果
    3）数据存储data文件
        存储原始的数据，以及中间产生的一些文件
    4）存储答案answer文件
        存储最后产生的答案
